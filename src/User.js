import React, { Component } from 'react';
import UserStyle from './User.module.css'
const User = (props) => {
    return (
        <h2 className={ UserStyle.txt}>Hi User</h2>
        )
}

export default User;